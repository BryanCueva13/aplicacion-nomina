@{
    ViewData["Title"] = "Error";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card border-danger">
            <div class="card-body text-center">
                <i class="bi bi-exclamation-triangle display-1 text-danger"></i>
                <h1 class="display-4 text-danger">Error</h1>
                <p class="lead">Ha ocurrido un error inesperado en la aplicación.</p>
                <hr>
                <p>
                    Por favor, intenta nuevamente o contacta al administrador del sistema si el problema persiste.
                </p>
                <a href="@Url.Action("Index", "Home")" class="btn btn-primary">
                    <i class="bi bi-house"></i> Volver al Dashboard
                </a>
            </div>
        </div>
    </div>
</div>
