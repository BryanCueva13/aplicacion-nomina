@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card border-danger">
            <div class="card-body text-center">
                <i class="bi bi-shield-exclamation display-1 text-danger"></i>
                <h3 class="card-title text-danger mt-3">Acceso Denegado</h3>
                <p class="card-text">
                    No tienes permisos suficientes para acceder a esta sección del sistema.
                </p>
                <p class="text-muted">
                    Si crees que esto es un error, contacta al administrador del sistema.
                </p>
                <a href="@Url.Action("Index", "Home")" class="btn btn-primary">
                    <i class="bi bi-house"></i> Volver al Dashboard
                </a>
            </div>
        </div>
    </div>
</div>
